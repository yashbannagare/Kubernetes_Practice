apiVersion: apps/v1
kind: Deployment
metadata:
  name: main-deployment
  labels: { app: main }
spec:
  replicas: 2
  selector:
    matchLabels: { app: main }
  template:
    metadata:
      labels: { app: main }
    spec:
      containers:
        - name: main
          image: 992382715314.dkr.ecr.us-east-1.amazonaws.com/main:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: main-service
spec:
  selector: { app: main }
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
