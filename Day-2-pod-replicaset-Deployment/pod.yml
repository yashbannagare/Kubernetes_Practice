apiVersion: v1
kind: Pod
metadata:
  name: web-server-pod
  labels:
    app: web
    environment: production
spec:
  containers:
  - name: nginx-container
    image: nginx:1.25
    ports:
    - containerPort: 80
